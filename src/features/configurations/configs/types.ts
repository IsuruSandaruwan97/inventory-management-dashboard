export type ProductsCategoriesDataType = {
  itemId: string;
  code: string;
  name: string;
  status: boolean;
};

export type ProductSubCategoriesDataType = {
  itemId: string;
  catName: string;
  code: string;
  name: string;
  status: boolean;
};
