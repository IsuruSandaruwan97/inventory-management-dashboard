import Time from '@components/Time';

export default {
  title: 'Components/Time',
  component: Time,
};

const Template = () => <Time />;

export const Default: any = Template.bind({});
Default.args = {};
